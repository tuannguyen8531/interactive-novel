{
  "world_builder": {
    "template": "world_builder.md",
    "semantic_version": "1.9.0",
    "input_contract": "world-builder-input",
    "output_schema_version": "world-seed",
    "required_variables": ["input_json"],
    "changelog": "Resolve example provenance versions from the prompt manifest."
  },
  "world_guide": {
    "template": "world_guide.md",
    "semantic_version": "1.1.0",
    "input_contract": "world-guide-input",
    "output_schema_version": "world-brief-suggestion",
    "required_variables": ["input_json"],
    "changelog": "Decouple rating and violence ceiling presets from creative brief ideation."
  },
  "planner": {
    "template": "planner.md",
    "semantic_version": "1.6.0",
    "input_contract": "role-input",
    "output_schema_version": "turn-plan",
    "required_variables": ["input_json"],
    "changelog": "Ground story time in explicit 24-hour clock context and distinguish elapsed minutes from HHMM."
  },
  "simulator": {
    "template": "simulator.md",
    "semantic_version": "1.11.0",
    "input_contract": "role-input",
    "output_schema_version": "simulation-result",
    "required_variables": ["input_json"],
    "changelog": "Develop accepted outcomes into visible interactive beats that leave room for the player's next response."
  },
  "validator": {
    "template": "validator.md",
    "semantic_version": "1.10.0",
    "input_contract": "role-input",
    "output_schema_version": "consistency-report",
    "required_variables": ["input_json"],
    "changelog": "Rename the logical role to validator."
  },
  "writer": {
    "template": "writer.md",
    "semantic_version": "1.9.0",
    "input_contract": "role-input",
    "output_schema_version": "narrative-draft",
    "required_variables": ["input_json", "language_guidance"],
    "changelog": "Write longer playable scenes with action-reaction progression and an unresolved interaction handoff."
  },
  "critic": {
    "template": "critic.md",
    "semantic_version": "1.8.0",
    "input_contract": "role-input",
    "output_schema_version": "critique-result",
    "required_variables": ["input_json", "language_guidance"],
    "changelog": "Evaluate scene development, approximate target length, and the final interactive handoff."
  },
  "repair": {
    "semantic_version": "1.1.0",
    "template": "repair.md",
    "required_variables": [
      "original_prompt",
      "repair_input_json"
    ],
    "changelog": "Remove repeated repair instructions while retaining the original context and boundaries."
  }
}
