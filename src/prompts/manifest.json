{
  "world_builder": {
    "template": "world_builder.md",
    "semantic_version": "1.7.0",
    "input_contract": "role-input",
    "output_schema_version": "world-seed",
    "required_variables": ["input_json"],
    "changelog": "Make adult_18_plus enable adult content directly while scene age and the violence ceiling remain authoritative."
  },
  "planner": {
    "template": "planner.md",
    "semantic_version": "1.5.0",
    "input_contract": "role-input",
    "output_schema_version": "turn-plan",
    "required_variables": ["input_json"],
    "changelog": "Use canonical current character locations over historical claims when grounding player intent."
  },
  "simulator": {
    "template": "simulator.md",
    "semantic_version": "1.8.0",
    "input_contract": "role-input",
    "output_schema_version": "simulation-result",
    "required_variables": ["input_json"],
    "changelog": "Ground physical actions in authoritative current character locations rather than superseded location claims."
  },
  "context_validator": {
    "template": "context_validator.md",
    "semantic_version": "1.7.0",
    "input_contract": "role-input",
    "output_schema_version": "consistency-report",
    "required_variables": ["input_json"],
    "changelog": "Validate realized spatial continuity against authoritative current locations instead of raw input or historical claims."
  },
  "writer": {
    "template": "writer.md",
    "semantic_version": "1.4.0",
    "input_contract": "role-input",
    "output_schema_version": "narrative-draft",
    "required_variables": ["input_json"],
    "changelog": "Keep current scene positions aligned with authoritative character locations."
  },
  "critic": {
    "template": "critic.md",
    "semantic_version": "1.4.0",
    "input_contract": "role-input",
    "output_schema_version": "critique-result",
    "required_variables": ["input_json"],
    "changelog": "Check current-scene location continuity against authoritative character positions."
  }
}
